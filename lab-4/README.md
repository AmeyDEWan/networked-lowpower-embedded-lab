# I2C sensor readings

A lab about sensors and reading from them via I2C. In nrf52840, DMA is used implicitely to put sensor data at peripheral in RAM(conveneient).
In Task3, implementated a FSM which responds to the orientation of the accelerometer and turns on different LED patterns. 